<% provide(:title, "Home") %>
<%= render 'layouts/slider' %>

<div class="items">
  <div class="container">
    <div class="row">
      <div class="span12">
        <h3 class="title">Latest Products</h3>
      </div>

			<% Product.last(6).reverse.each do |product| %>
				<%= render "shared/product", :product => product %>
			<% end %>

    </div>
  </div>
</div>